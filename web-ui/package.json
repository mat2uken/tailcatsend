{
  "name": "ponlet-web-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --config oxlint.config.js src/ tests/",
    "format": "oxfmt --write src/ tests/",
    "format:check": "oxfmt --check src/ tests/"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "vanjs-core": "^1.6.1"
  },
  "devDependencies": {
    "@nkzw/oxlint-config": "^2.0.1",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.14.3",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "typescript": "^5.7.0",
    "vite": "^8.3.0",
    "vitest": "^4.1.11"
  }
}
