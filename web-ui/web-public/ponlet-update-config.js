// Pages can replace this file during release publishing with the signed
// manifest URL, signature URL, public key, and current revision. Keeping the
// default empty makes local and offline builds use the bundled version.
globalThis.__PONLET_UPDATE_CONFIG__ ??= undefined;
