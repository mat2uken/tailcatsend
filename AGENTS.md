# 共通作業方針

明示的な指示がない限り、作業は適切な単位でサブエージェントに分割して作業を行い、それを最終的に統合する形で進める
サブエージェント、もしくは、リモートの別のCodexのセッションとのやりとりをする場合は、作業にかかると予測される時間の2倍程度のタイムアウトにして、頻繁に確認しない

## 作業と許可

Gitで管理されたプロジェクトを扱うときは、対象リポジトリ、ブランチ、HEAD、worktreeの変更を確認する。読み取り専用の依頼ではファイル、worktree、Git、実機を変更しない。
ユーザーが許可した範囲の後続作業は、同じ確認を繰り返さず進める。許可範囲外の変更が必要になった場合だけ、理由と最小案を示して止める。

## Git

新しい branch は `feature/<short-kebab-case-description>` を既定とし、ユーザーまたは依頼が指定した名前を優先する。
commit、branch、trailer、credit に `agent`、`chatgpt`、`AI` などの語を含めない。
cleanup、branch整理、削除の依頼を、指定されていない branch の merge へ広げない。merge、rebase、branch削除、履歴変更、force-push は対象と目的を明示し、許可された対象だけ行う。
commit と push は別の操作として扱う。

## 説明

説明は自然な日本語で行い、慣用的な表現を避ける。「契約」「contract」「境界」「resource」「資産」は使わない。
API、引用、ファイル名など正確な表記が必要な場合は原文を保ち、その他の英語も無理に日本語へ訳さない。
確認できた事実、推測、未検証の項目を分ける。過去の検証結果を、現在のSHAや端末での成功として扱わない。

